# ═══════════════════════════════════════════════════════════════
# .env.example
#
# Copy to .env and fill in YOUR actual values.
# NEVER commit .env to git!
# ═══════════════════════════════════════════════════════════════

# Environment: development or production
ENVIRONMENT=development
DEBUG=True

# App Secret Key (used for JWT signing - REQUIRED)
# auth.py reads SECRET_KEY (not JWT_SECRET_KEY)
SECRET_KEY=generate-a-long-random-string-here

# PostgreSQL Database on Railway
# Format: postgresql://user:password@host:port/dbname
DATABASE_URL=postgresql://user:password@host:port/railway

# Razorpay Payment Gateway
RAZORPAY_KEY_ID=rzp_test_xxxxx
RAZORPAY_KEY_SECRET=your_secret

# Email / SMTP Configuration (REQUIRED for password reset emails)
SMTP_USER=your-gmail@gmail.com
SMTP_PASS=your-gmail-app-password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
RESET_BASE_URL=https://pickmyseat.in

# FastAPI Server
FASTAPI_HOST=0.0.0.0
FASTAPI_PORT=8000

# Frontend & API URLs
FRONTEND_URL=http://localhost:3000
API_BASE_URL=http://localhost:8000/api

# ML Model Paths
RANK_MODEL_PATH=backend/data/cleaned/rank_model_final.pkl
CUTOFF_MODEL_PATH=backend/data/cleaned/cutoff_predictor.pkl
COLLEGE_CODES_PATH=backend/data/cleaned/college_codes.csv
COURSE_CODES_PATH=backend/data/cleaned/course_codes.csv
